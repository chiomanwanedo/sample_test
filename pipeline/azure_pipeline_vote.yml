trigger:
  branches:
    include:
      - main

resources:
  repositories:
    - repository: apprepo
      type: github
      name: chiomanwanedo/example-voting-app
      ref: main
      endpoint: github.com_chiomanwanedo

variables:
  IMAGE_TAG: $(Build.BuildId)

stages:
  - stage: BuildAndPush
    displayName: Build and Push Docker Images to ACR
    jobs:
      - job: PushImages
        displayName: Build & Push vote, result, and worker
        pool:
          name: Default  # Change to your actual agent pool if different

        steps:
          - checkout: apprepo
            path: example-voting-app  # ✅ This prevents nested folder issues

          - task: Docker@2
            displayName: Build and Push 'vote' image
            inputs:
              containerRegistry: 'dockeracr'  # Replace with your ACR connection name
              repository: 'vote'
              command: 'buildAndPush'
              Dockerfile: 'example-voting-app/vote/Dockerfile'
              buildContext: 'example-voting-app/vote'
              tags: |
                $(IMAGE_TAG)

          - task: Docker@2
            displayName: Build and Push 'result' image
            inputs:
              containerRegistry: 'dockeracr'
              repository: 'result'
              command: 'buildAndPush'
              Dockerfile: 'example-voting-app/result/Dockerfile'
              buildContext: 'example-voting-app/result'
              tags: |
                $(IMAGE_TAG)

          - task: Docker@2
            displayName: Build and Push 'worker' image
            inputs:
              containerRegistry: 'dockeracr'
              repository: 'worker'
              command: 'buildAndPush'
              Dockerfile: 'example-voting-app/worker/Dockerfile'
              buildContext: 'example-voting-app/worker'
              tags: |
                $(IMAGE_TAG)
